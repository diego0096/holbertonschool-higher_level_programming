#!/usr/bin/python3
def search_replace(my_list, search, replace):
    a = []
    for a in my_list:
        if a == search:
            a.append(replace)
        else:
            a.append(n)
    return new
